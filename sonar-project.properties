# Required metadata
sonar.projectKey=CacheFramework
sonar.projectName=CacheProject
sonar.projectVersion=3.0

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Language
sonar.language=java

# Encoding of the source files
sonar.sourceEncoding=UTF-8
